# [defaults]
# remote_user = ubuntu
# remote_tmp = /tmp/.ansible

[defaults]
remote_user = ubuntu

# FIX: Use the user's home directory for temporary files (~/.ansible/tmp).
# This prevents "Permission denied" errors in the shared /tmp directory
# caused by previous runs executing with different privileges (e.g., root).
remote_tmp = ~/.ansible/tmp